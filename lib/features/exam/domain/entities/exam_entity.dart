class ExamEntity {
  final int id;
  final String title;
  final String description;
  final int questionCount;

  const ExamEntity({
    required this.id,
    required this.title,
    required this.description,
    required this.questionCount,
  });
}